<Window x:Class="VisualHFT.View.KalshiStrikeLadderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        mc:Ignorable="d"
        Title="Kalshi — Strike Ladder"
        Height="600" Width="950"
        Background="#FF1E1E1E"
        TextElement.Foreground="White"
        FontFamily="Consolas"
        FontSize="13">
    <Window.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#FF252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#FF1F1F1F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="#FF2D2D30" Padding="10,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🎯 Kalshi Strike Ladder" FontSize="16" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="   live top-of-book by event/strike, prices in cents (= implied YES probability)"
                           Foreground="#FF999999" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBlock Text="   rows: " Foreground="#FF999999" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBlock Text="{Binding Strikes.Count}" Foreground="#FFCCCCCC" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="StrikesGrid"
                  ItemsSource="{Binding Strikes}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserSortColumns="True"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FF333333"
                  HeadersVisibility="Column"
                  Background="#FF1E1E1E"
                  BorderThickness="0"
                  AlternationCount="2"
                  MouseDoubleClick="StrikesGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cat"     Binding="{Binding Category}"    Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF8AB4F8"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Event"   Binding="{Binding EventTicker}" Width="180" />
                <DataGridTextColumn Header="Strike"  Binding="{Binding Strike}"      Width="80" />
                <DataGridTextColumn Header="Ticker"  Binding="{Binding Ticker}"      Width="200" />
                <DataGridTextColumn Header="YES Bid" Binding="{Binding YesBid, StringFormat='{}{0:F1}¢'}"  Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FF66BB6A"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="YES Ask" Binding="{Binding YesAsk, StringFormat='{}{0:F1}¢'}"  Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FFEF5350"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Spread"  Binding="{Binding Spread, StringFormat='{}{0:F1}¢'}"  Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FF999999"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Mid Prob" Binding="{Binding MidProb, StringFormat='{}{0:F1}%'}"  Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FFFFD54F"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Updated" Binding="{Binding LastUpdate, StringFormat='HH:mm:ss'}"  Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FF777777"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
